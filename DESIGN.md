# DeployHub Design System

이 문서는 Raycast 마케팅 페이지 분석을 대체한다. 마케팅 페이지는 DeployHub의 제품 의미와 정보 구조를 결정할 올바른 디자인 권위가 아니었고, 그 분석을 남겨 두면 이후 작업자와 AI가 장식적인 브랜드 관습을 제품 규칙으로 오해하게 된다. 이 문서와 실제 코드의 토큰이 DeployHub 디자인의 기준이다.

## 방향

DeployHub는 어두운 제도판 위에 시스템의 선언과 관측을 대조하는 도구다. 화면은 홍보용 무대가 아니라 운영 사실을 읽는 도면이어야 한다. 깊이는 여러 단계의 카드 색으로 만들지 않고 종이, 선, 여백, 타이포그래피 위계로 만든다.

- 페이지 바탕은 단색 `--canvas`다.
- 도면 블록은 `--paper` 위에 선과 측정값을 놓는다.
- 카드와 중첩 패널의 경계는 `--rule`을 사용한다.
- 그림자, 광택, 장식용 그라디언트로 계층을 만들지 않는다.
- 색은 실제 상태 또는 상호작용을 전달할 때만 사용한다.

## 선언과 관측

선언은 manifest가 말하는 의도이고, 관측은 연결된 제공자에서 측정한 사실이다. 둘은 언제나 같은 순서로 표기한다.

```text
선언 ┆ 관측
```

`┆`는 단순 장식이 아니라 서로 다른 출처를 분리하는 규칙이다. 어느 한쪽이 없더라도 다른 쪽 값으로 채우지 않는다.

| 상황 | 표기 | 판정과 표현 |
|---|---|---|
| 관측 없음 | `선언값 ┆ 미확인` | 관측 부재를 `--absent`로 표시한다. 선언값을 관측값처럼 복사하지 않는다. |
| 선언 없음 | `미선언 ┆ 관측값` | 선언 부재를 `--absent`로 표시한다. 관측 사실은 그대로 보존한다. |
| Drift | `선언값 ┆ 관측값` | 두 값을 모두 보여 주고 차이를 설명한다. Drift 자체에는 경고색을 쓰지 않는다. |

Drift는 두 출처가 다르다는 사실이며 곧바로 주의나 장애를 뜻하지 않는다. 별도의 검증 규칙이 실제 오류를 판정한 경우에만 `--fault`, 주의가 필요한 실제 상태에만 `--caution`을 사용한다.

## 토큰

아래 목록이 완전한 토큰 집합이다. 비슷한 배경색이나 임의의 의미 색을 추가하지 않는다.

| 토큰 | 값 | 용도 |
|---|---:|---|
| `--canvas` | `#07080a` | 단색 페이지 배경과 모바일 오버레이의 기반 |
| `--paper` | `#0d0d0d` | 카드, 입력, 도면 블록의 종이 표면 |
| `--grid` | `#16181a` | `.sheet` 내부의 32px 제도 격자 |
| `--rule` | `#242728` | 카드 경계, 표 행, 구획선 |
| `--line` | `#f4f4f6` | 제목, 핵심 라벨, 가장 강한 선 |
| `--line-mute` | `#cdcdcd` | 본문과 기본 전경 |
| `--annotation` | `#9c9c9d` | 설명, 메타데이터, 보조 라벨 |
| `--absent` | `#6a6b6c` | 미확인·미선언·자리표시자와 최저 강조 구분자 |
| `--fault` | `#ff6161` | 검증된 오류, 장애, 파괴적 결과 |
| `--caution` | `#ffc533` | 실제 주의 상태와 검토가 필요한 경고 |
| `--confirm` | `#59d499` | 성공 피드백, diff 추가, 활성 상태 |
| `--accent` | `#57c1ff` | 링크와 키보드 포커스 같은 상호작용 |
| `--radius-card` | `10px` | 카드와 도면 블록 |
| `--radius-button` | `8px` | 버튼, 입력, 배지, 행 선택 영역 |
| `--font-sans` | `Inter, "Noto Sans KR", system-ui, sans-serif` | 제목, 본문, 라벨 등 산문 |
| `--font-mono` | `ui-monospace, "SF Mono", Menlo, Consolas, monospace` | 측정값, 식별자, 시각, 코드 |

`--confirm`과 `--accent`는 상태색을 늘리기 위한 장식색이 아니다. `--confirm`은 기존 성공·diff 추가·활성 피드백에만, `--accent`는 링크와 포커스에만 사용한다.

## 타이포그래피

측정된 값, 식별자, 버전, 해시, 컨테이너 이름, 시각은 `--font-mono`로 쓴다. 문장, 제목, 설명, 동작 라벨은 `--font-sans`로 쓴다. 폰트는 운영체제에 설치된 글꼴만 사용하며 네트워크에서 내려받지 않는다.

| 크기 | 용도 |
|---:|---|
| `20px` | 페이지 제목과 최상위 섹션 제목 |
| `15px` | 강조 본문과 주요 행 제목 |
| `14px` | 기본 본문, 버튼, 입력 |
| `13px` | 보조 본문과 표 셀 |
| `12px` | 주석, 메타데이터, 배지 |

20px보다 큰 디스플레이 타입은 사용하지 않는다. 크기보다 명도, 굵기, 간격으로 위계를 만든다.

## 격자와 표면

격자는 도면을 읽는 데 도움이 되는 블록에서만 `.sheet` 클래스로 적용한다. `.sheet`는 `--paper` 바탕 위에 `--grid`로 32px 격자를 그린다. `html`, `body`, 페이지 셸, 전체 화면 배경에는 격자를 적용하지 않으며 페이지 바탕은 항상 민무늬다.

표면은 `--paper` 하나만 사용한다. 부모와 자식이 같은 표면을 공유하면 `--rule` 테두리로 경계를 유지한다. hover와 selected 상태는 새 배경 토큰을 만들지 않고 `white/[0.02]`처럼 매우 옅은 투명 밝기를 사용한다.

## 상태와 상호작용

- 정상 상태는 색을 갖지 않는다. 기본 텍스트와 선만으로 표시한다.
- Drift는 경고색을 갖지 않는다. 선언과 관측의 차이를 나란히 보여 준다.
- 오류와 장애는 `--fault`, 실제 주의 상태는 `--caution`을 사용한다.
- 성공 피드백과 diff 추가는 `--confirm`을 유지한다.
- 링크와 포커스 링은 `--accent`를 유지한다.
- 색만으로 의미를 전달하지 않고 텍스트 라벨이나 구조를 함께 제공한다.

## 금지 사항

- 정상 상태에 의미 색을 부여하지 않는다.
- Drift를 경고색으로 칠하지 않는다.
- 누락된 관측값을 선언값으로 채우지 않는다.
- 웹 폰트, `@font-face`, 원격 폰트 CSS, `next/font`를 추가하지 않는다.
- 라이트 모드나 자동 색상 모드를 추가하지 않는다.
- 페이지 전체에 격자를 깔지 않는다.
- 새 표면 단계, 그림자, 장식용 상태색을 추가하지 않는다.
