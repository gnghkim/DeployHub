# Normalize line endings on commit, check out native on Windows
* text=auto

# Files that must stay LF — they run inside Linux containers
*.sh          text eol=lf
Dockerfile    text eol=lf
*.Dockerfile  text eol=lf
Caddyfile     text eol=lf
*.yml         text eol=lf
*.yaml        text eol=lf
